{
  "name": "cxps_mail_link_starter",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "deploy:stg": "npx --yes firebase-tools@14 deploy --only hosting:staging",
    "deploy:prod": "echo \"? 本番は main マージで自動反映。ローカルからはデプロイ禁止\" && exit 1"
  },
  "engines": {
    "node": ">=18"
  }
}
{
  "scripts": {
    "deploy:stg": "powershell -ExecutionPolicy Bypass -File ./deploy.ps1 stg",
    "deploy:prod": "powershell -ExecutionPolicy Bypass -File ./deploy.ps1 prod"
  }
}
